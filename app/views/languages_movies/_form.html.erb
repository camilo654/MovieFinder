<%= form_with(model: languages_movie, local: true) do |form| %>
  <% if languages_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(languages_movie.errors.count, "error") %> prohibited this languages_movie from being saved:</h2>

      <ul>
      <% languages_movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
