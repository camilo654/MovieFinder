<%= form_with(model: directors_movie, local: true) do |form| %>
  <% if directors_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(directors_movie.errors.count, "error") %> prohibited this directors_movie from being saved:</h2>

      <ul>
      <% directors_movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
